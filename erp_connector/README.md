# erp_connector

**Status:** placeholder — no code.

Target: 鼎新 (DSC) T100 / B2 / Workflow ERP — the dominant Taiwan-mid-market
ERP stack. Also Workday / SAP S/4HANA for larger customers.

Planned shape: ``list_purchase_orders()``, ``get_invoice(invoice_id)``,
``post_journal_entry(...)`` — read-only first, write only after customer
sign-off on audit trail integration.

Activates: M4 W13-14 (~2026-08) or first real ERP customer.
